# Generated by skill-generator/scripts/emit_tests.py.
# Re-run the emitter after the SKILL.md changes; do not edit by hand.
schema_version: 1
slug: asset-harvester
items:
- id: asset-harvester.001
  category: single-tool
  tools_required:
  - asset-harvester
  user_prompt: 'I want to install NVIDIA Asset Harvester from scratch on a

    fresh Ubuntu 22.04 box. What''s the recommended one-shot

    install path, and what repo do I clone?

    '
  timeout_seconds: 90
  expected_signals:
  - github.com/NVIDIA/asset-harvester
  - setup.sh
  - conda activate asset-harvester
  forbidden_signals:
  - gitlab-master.nvidia.com
  judge_rubric:
  - The response clones the public github.com/NVIDIA/asset-harvester repo.
  - The response runs bash setup.sh to create the conda env.
  - The response does not recommend an internal NVIDIA mirror or GitLab URL.
- id: asset-harvester.002
  category: single-tool
  tools_required:
  - asset-harvester
  user_prompt: 'Why does the Asset Harvester install pin gsplat to a specific

    commit, and how do I install it correctly if I''m doing a manual

    (non-setup.sh) install?

    '
  timeout_seconds: 90
  expected_signals:
  - b60e917c95afc449c5be33a634f1f457e116ff5e
  - gsplat
  - --no-build-isolation
  expected_signals_any:
  - before the editable install
  - CUDA ABI
  - wrong CUDA
  forbidden_signals:
  - gitlab-master.nvidia.com
  - 'pip install gsplat

    '
  judge_rubric:
  - The response installs gsplat from source at the pinned commit b60e917.
  - The response explains gsplat must be installed before the editable asset-harvester install (or equivalent
    ABI/ordering reason).
- id: asset-harvester.003
  category: single-tool
  tools_required:
  - asset-harvester
  user_prompt: 'How do I download the Asset Harvester model checkpoints, and

    what files should end up in my checkpoints/ directory?

    '
  timeout_seconds: 90
  expected_signals:
  - hf auth login
  - hf download nvidia/asset-harvester
  - AH_multiview_diffusion.safetensors
  - AH_tokengs_lifting.safetensors
  - AH_camera_estimator.safetensors
  - AH_object_seg_jit.pt
  forbidden_signals:
  - gitlab-master.nvidia.com
  judge_rubric:
  - The response points the user to the public huggingface.co nvidia/asset-harvester repo.
  - The response instructs the user to authenticate with an HF token before downloading.
  - The response lists all four AH_* checkpoint files.
- id: asset-harvester.004
  category: single-tool
  tools_required:
  - asset-harvester
  user_prompt: 'I have a single image of a car (frame.jpeg) with no camera

    calibration and no mask. I want to turn it into a 3D Gaussian

    Splat. What''s the full workflow?

    '
  timeout_seconds: 180
  expected_signals:
  - AH_object_seg_jit.pt
  - image_segment
  - run_inference.py
  - --ahc_checkpoint
  - --image_dir
  expected_ordering:
  - image_segment
  - run_inference.py
  forbidden_signals:
  - gitlab-master.nvidia.com
  - --data_root
  judge_rubric:
  - The response first generates mask.png via the image_segment utility.
  - The response then runs run_inference.py with the camera estimator checkpoint (--ahc_checkpoint).
  - The response uses --image_dir (not --data_root) since this is single-view mode.
- id: asset-harvester.005
  category: single-tool
  tools_required:
  - asset-harvester
  user_prompt: 'I only have an 8 GB GPU. I''m getting CUDA out-of-memory when

    running run_inference.py on the bundled samples. What flag do I

    add?

    '
  timeout_seconds: 60
  expected_signals:
  - --offload_model_to_cpu
  forbidden_signals:
  - gitlab-master.nvidia.com
  judge_rubric:
  - The response recommends --offload_model_to_cpu for run_inference.py.
  - The response notes the 16 GB VRAM floor / CPU offload trade-off (slower).
- id: asset-harvester.006
  category: single-tool
  tools_required:
  - asset-harvester
  user_prompt: 'Walk me through the end-to-end pipeline for turning an NCore V4

    driving log clip into per-track 3D Gaussian assets. What are the

    two steps and what commands do I run?

    '
  timeout_seconds: 180
  expected_signals:
  - run_ncore_parser.sh
  - run.sh
  - --component-store
  - --data-root
  expected_ordering:
  - run_ncore_parser.sh
  - run.sh
  forbidden_signals:
  - gitlab-master.nvidia.com
  judge_rubric:
  - The response runs scripts/run_ncore_parser.sh first with --component-store pointing at the clip.
  - The response then runs run.sh with --data-root pointing at the parser output directory.
  - The response indicates gaussians.ply is produced per sample.
- id: asset-harvester.007
  category: single-tool
  tools_required:
  - asset-harvester
  user_prompt: 'I want to evaluate the reconstructed assets against ground-truth

    views using benchmark/eval.py. Why can''t I just run it inside my

    existing asset-harvester conda env?

    '
  timeout_seconds: 90
  expected_signals:
  - av-object-benchmark
  - transformers
  - conda create
  - --clone asset-harvester
  expected_signals_any:
  - '4.56'
  - 4.48.3
  forbidden_signals:
  - gitlab-master.nvidia.com
  judge_rubric:
  - The response explains the transformers version conflict between the main env (4.48.3) and benchmark
    (>=4.56.0).
  - The response instructs cloning the env as av-object-benchmark and running benchmark/install.sh.
- id: asset-harvester.008
  category: single-tool
  tools_required:
  - asset-harvester
  user_prompt: 'I''ve produced gaussians.ply files and now want to insert them as

    objects in NVIDIA Omniverse NuRec. What do I need to do before

    handing the assets to NuRec, and are there gotchas?

    '
  timeout_seconds: 180
  expected_signals:
  - orient_gaussians_for_nurec
  - metadata.yaml
  - generate_external_assets_metadata
  - PPISP
  forbidden_signals:
  - gitlab-master.nvidia.com
  judge_rubric:
  - The response rotates the Gaussians via orient_gaussians_for_nurec.
  - The response generates metadata.yaml for the NuRec external-assets input.
  - The response warns to disable PPISP when reconstructing the scene in NuRec.
  - The response notes Asset Harvester does not predict scale — NuRec reads scale from the source clip
    cuboid.
- id: asset-harvester.009
  category: single-tool
  tools_required:
  - asset-harvester
  user_prompt: 'A customer wants to use Asset Harvester to reconstruct indoor

    furniture from a few photos in their living room. Is this a

    good fit?

    '
  timeout_seconds: 60
  expected_signals_any:
  - out of distribution
  - out of domain
  - AV
  - autonomous vehicle
  forbidden_signals:
  - gitlab-master.nvidia.com
  judge_rubric:
  - The response says Asset Harvester is trained for AV objects only and will not work reliably on indoor
    furniture.
  - The response does not falsely claim the tool supports general-purpose text-to-3D or indoor scans.
- id: asset-harvester.010
  category: single-tool
  tools_required:
  - asset-harvester
  user_prompt: 'I''m running a smoke test on just 5 objects and want multiview

    images only — no Gaussian .ply output. Which run.sh flags do I

    use?

    '
  timeout_seconds: 60
  expected_signals:
  - --max-samples
  - --skip-lifting
  - run.sh
  forbidden_signals:
  - gitlab-master.nvidia.com
  judge_rubric:
  - The response uses --max-samples 5 (or similar) to limit to 5 objects.
  - The response uses --skip-lifting so only multiview outputs are written.
- id: asset-harvester.011
  category: single-tool
  tools_required:
  - asset-harvester
  user_prompt: 'Before I run setup.sh, is there a way to check my host has

    everything Asset Harvester needs (driver, conda, GCC, HF token)

    without hitting the network?

    '
  timeout_seconds: 60
  expected_signals:
  - scripts/validate_setup.py
  expected_signals_any:
  - driver
  - '570'
  - HF_TOKEN
  forbidden_signals:
  - gitlab-master.nvidia.com
  judge_rubric:
  - The response points the user to scripts/validate_setup.py.
  - 'The response mentions at least one of: driver >= 570, conda, GCC 10-13, HF_TOKEN.'
- id: asset-harvester.012
  category: single-tool
  tools_required:
  - asset-harvester
  user_prompt: 'I got a 401 Unauthorized when running

    `hf download nvidia/asset-harvester`. What''s wrong?

    '
  timeout_seconds: 60
  expected_signals:
  - hf auth login
  expected_signals_any:
  - model card
  - accept
  - terms
  - HF_TOKEN
  forbidden_signals:
  - gitlab-master.nvidia.com
  judge_rubric:
  - The response instructs the user to accept the model-card terms on the nvidia/asset-harvester HF page.
  - The response instructs the user to authenticate with hf auth login using a token from huggingface.co/settings/tokens.
generated_at: '2026-05-08T23:07:28Z'
generated_with:
  provider: openai-compatible
  model: aws/anthropic/bedrock-claude-opus-4-7
  generator_branch: main
  generator_commit: e08b1b2
